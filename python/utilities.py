""" 
utility functions module:

no code here, only imports and reexports. So if isEven is in isEven.py, this file imports it as 
from isEven import isEven. Then this should be available as utilities.isEven in any code importing this.
"""

from isEven import isEven
from divides import divides
from multiplesList import multiplesList
from isPrime import isPrime, primes, prime, factor, sumofprimes